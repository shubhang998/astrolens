services:
  api:
    build:
      context: .
    command: uvicorn astrolens.api.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      ASTROLENS_ENV: local
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
